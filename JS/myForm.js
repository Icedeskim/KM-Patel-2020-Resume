


$alert( jQuery.data( document.getContactData, "Form was submitted"));

if getContactData( ["name", "email", "phone", "reason for contacting", "comments"]);
else {
    $alert ("please complete contact form");
}
$removeClass( button);
